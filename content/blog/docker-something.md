# docker 指令

# 查看容器占用的空間

docker ps -as

# 刪除特定容器

docker rm <container-id> <container-id>

# 或一次清理所有停止的容器

docker container prune

- 查看日誌：docker logs <container-id>
- 重新啟動：docker start <container-id>
- 檢查配置：docker inspect <container-id>

# 查看詳細的空間使用情況

docker system df

# 查看更詳細的資訊

docker system df -v

會顯示：

- Images 占用空間
- Containers 占用空間
- Volumes 占用空間
- Build Cache 占用空間

🧹 大掃除指令

如果 Docker 占用太多空間，一次清理所有未使用的資源：

# 清理所有未使用的資源（危險！會刪除停止的容器、未使用的 image、build cache）

docker system prune -a

# 更安全的版本（只清理停止的容器和懸空的 image）

docker system prune

---

關於設定自動清理

下次運行容器時加上 --rm 參數：
docker run --rm -p 3005:8080 --env-file .env myproject-backend

效果： 容器停止後自動刪除，不會留下垃圾

適合： 測試、一次性任務

不適合： 需要保留日誌或重新啟動的容器
